@page "/"

<PageTitle>MediView</PageTitle>

<h1>MediView</h1>

<AuthorizeView>
    <Authorized>
        <p>Signed in as @context.User.Identity?.Name. Pick a page from the menu.</p>
    </Authorized>
    <NotAuthorized>
        <p><a href="login">Sign in</a> to continue.</p>
    </NotAuthorized>
</AuthorizeView>
